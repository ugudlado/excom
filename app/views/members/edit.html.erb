<h1>Editing Member</h1>

<%= render 'form', member: @member %>

<% if current_member.member_type_id == MemberType.find_by(name: 'Admin').id %>
    <%= button_to 'Show', @member, :method => 'get', :class => "btn btn-success" %>
    <%= button_to 'Back', members_path, :method => 'get', :class => "btn btn-default" %>
<% end %>